/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // Si usas la carpeta `app`, habilita esta opción
    },
  };

module.exports = nextConfig;
  